name: ↩️ Restauration à partir de la dernière sauvegarde

on:
  workflow_dispatch:
    # Permet de déclencher le workflow manuellement

jobs:
  restore:
    runs-on: ubuntu-latest
    permissions: # <-- CORRECTION DE LA PERMISSION
      contents: write # Accorde la permission d'écrire au jeton GITHUB_TOKEN
      
    steps:
      - name: ⬇️ Checkout du code
        uses: actions/checkout@v4
        
      - name: 🔍 Trouver la dernière sauvegarde (Archive)
        id: find_latest
        run: |
          # Trouve la dernière archive .tar.gz par date
          LATEST_BACKUP=$(ls -td saves/backup-*.tar.gz | head -1)
          
          if [ -z "$LATEST_BACKUP" ]; then
            echo "::error::Aucune archive de sauvegarde trouvée dans 'saves/'."
            exit 1
          fi
          
          echo "Dernière archive trouvée : $LATEST_BACKUP"
          echo "RESTORE_ARCHIVE=$LATEST_BACKUP" >> $GITHUB_OUTPUT

      # L'étape de nettoyage de la racine reste la même.

      - name: 📥 Restauration et Décompression des fichiers
        run: |
          RESTORE_ARCHIVE="${{ steps.find_latest.outputs.RESTORE_ARCHIVE }}"
          
          if [ ! -f "$RESTORE_ARCHIVE" ]; then
            echo "::error::L'archive de sauvegarde '$RESTORE_ARCHIVE' n'existe pas."
            exit 1
          fi
          
          # Décompresse l'archive à la racine (cette commande extrait les fichiers)
          tar -xzvf "$RESTORE_ARCHIVE" -C .
          
          echo "Restauration effectuée à partir de $RESTORE_ARCHIVE"

          
      - name: 🚀 Déploiement Final du Site Statique
        # Cette action va explicitement dire à GitHub de mettre à jour les Pages
        uses: actions/deploy-pages@v4 
        # C'est l'action standard et recommandée pour le déploiement de Pages.
        
        # Le 'needs: commit' assure que l'action ne se lance que si la restauration a eu lieu.
        with:
          # Note: Nous ne spécifions pas de chemin 'source' ou 'artifact' car l'action 
          # est configurée pour prendre le contenu de la branche 'main' (ou master) 
          # si l'option 'source' est définie sur 'main' dans les paramètres de Pages.
          # L'action utilise le jeton de Pages pour déclencher la mise à jour.
          pass-metadata: true 
