name: Déploiement Statique

on:
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    steps:
      - name: ⬇️ Checkout du code
        uses: actions/checkout@v4

      - name: ⚙️ Préparation des dossiers et nom de l'archive
        id: setup
        run: |
          # Crée le dossier saves s'il n'existe pas
          mkdir -p saves

          # Définit le nom de l'archive de sauvegarde
          SAVE_ARCHIVE="saves/backup-$(date +%Y%m%d-%H%M%S).tar.gz"
          echo "SAVE_ARCHIVE=$SAVE_ARCHIVE" >> $GITHUB_OUTPUT

      - name: 📦 Création et compression de la sauvegarde
        run: |
          SAVE_ARCHIVE="${{ steps.setup.outputs.SAVE_ARCHIVE }}"
          
          # Crée une archive tar.gz de tous les fichiers à la racine, 
          # EXCLUT les dossiers système et de travail.
          tar -czvf "$SAVE_ARCHIVE" \
            --exclude .git \
            --exclude .github \
            --exclude saves \
            --exclude dev \
            --exclude "$(basename "$SAVE_ARCHIVE")" \
            .
            
      - name: 🗑️ Nettoyage de la racine après compression
        run: |
          # Supprime tous les fichiers à la racine (sauf les dossiers de travail)
          ls -A | grep -v -E "^\.(git|github)$|^saves$|^dev$" | xargs rm -rf
            
      - name: 📥 Déplacement des fichiers de dev vers la racine
        run: |
          # Copie tout le contenu du dossier 'dev' vers la racine du dépôt
          cp -r dev/* .

      - name: 💾 Commit et Push des changements
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          
          # Ajoute tous les changements (sauvegardes, nouveaux fichiers)
          git add .
          
          if git diff --staged --quiet; then
            echo "Pas de changements à commiter."
          else
            git commit -m "🤖 Auto-déploiement: Mise à jour depuis 'dev' et sauvegarde"
            git push
          fi
          
